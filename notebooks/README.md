# Notebooks

Drop exploratory notebooks here when crafting portfolio visuals. Suggested flow:

1. Load `data/processed/airway_expression_long.csv`.
2. Prototype additional charts (e.g., enrichment plots, 3D PCA).
3. Export static figures to `docs/` for embedding in proposals.

